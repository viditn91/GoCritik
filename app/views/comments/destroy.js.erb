<% if flash.now[:alert].present? %>
  alert(<%= flash.now[:alert] %>);
<% else %>
  var review_container = $('#review_<%= @comment.review.id %>_sub_container');
  review_container.html("<%= escape_javascript render(@comment.review) %>");
<% end %>