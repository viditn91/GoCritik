<% if user_signed_in? && (rating = current_user.ratings.find_by(resource_id: @resource.id)) %>
  <%= rateit_tag(max: 5, min: 0, step: 0.5, value: rating.value, resetable: false) %>
<% else %>
  <%= rateit_tag(max: 5, min: 0, step: 0.5, value: 0, resetable: false) %>
<% end %>