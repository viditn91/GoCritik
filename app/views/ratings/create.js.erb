<% if flash.now[:alert].present? %>
  alert(<%= flash.now[:alert] %>);
<% else %>
  $('#resource-header #votes').html("<%= @resource.ratings.count %>");
  $('#resource-header #score').html("<%= @resource.calc_avg_rating %>");
<% end %>